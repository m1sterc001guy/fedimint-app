name: ecashapp
description: "A Fedimint Wallet"
publish_to: 'none'
version: 0.11.0-alpha

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_rust_bridge: 2.9.0
  qr_code_scanner_plus: ^2.1.1
  qr_flutter: ^4.1.0
  intl: ^0.20.0
  numpad_layout: ^0.0.3
  flutter_animate: ^4.5.2
  http: ^1.3.0
  lottie: ^3.3.1
  flutter_speed_dial: ^7.0.0
  smooth_page_indicator: ^1.2.1
  url_launcher: ^6.3.1
  path_provider: ^2.1.5
  freezed_annotation: ^3.0.0
  uuid: ^4.5.1
  crypto: ^3.0.6
  collection: ^1.19.1
  shimmer: ^3.0.0
  package_info_plus: 8.3.1
  provider: ^6.1.2
  flutter_foreground_task: ^8.13.0
  permission_handler: ^11.3.1
  app_links: ^6.3.3
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  vector_map_tiles: ^8.0.0
  geolocator: ^14.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mocktail: ^1.0.4
  freezed: ^3.0.6
  build_runner: ^2.4.15
  flutter_launcher_icons: ^0.14.4

flutter:
  generate: true
  uses-material-design: true
  assets:
    - assets/images/fedimint.png
    - assets/images/fedimint-icon-color.png
    - assets/images/ecash-app.png
    - assets/images/nostr.png
    - assets/animations/lightning.json

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/ecash-app.png"
  adaptive_icon_background: "#000000"
  adaptive_icon_foreground: "assets/images/ecash-app.png"
  # The App Store rejects a marketing icon with an alpha channel (error 90717).
  # iOS applies its own rounded mask, so filling the transparent corners with
  # the artwork's own edge colour is invisible on device. Android is unaffected
  # by these two keys, and must keep its alpha: adaptive icon foregrounds
  # require transparency.
  remove_alpha_ios: true
  background_color_ios: "#4AD5FF"
